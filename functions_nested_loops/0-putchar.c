#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char mot[] = "_putchar";
int i;
for (i = 0; mot[i] != '\0'; i++)
_putchar(mot[i]);
_putchar('\n');
return (0);
}
